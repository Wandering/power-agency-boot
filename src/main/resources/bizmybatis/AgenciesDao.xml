<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.power.dao.AgenciesDao">

	<!-- 可根据自己的需求，是否要使用 -->
	<resultMap type="com.power.entity.AgenciesEntity" id="agenciesMap">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="abbrCode" column="abbr_code" />
		<result property="region" column="region" />
		<result property="address" column="address" />
		<result property="status" column="status" />
		<result property="type" column="type" />
		<result property="parent" column="parent" />
		<result property="createDt" column="create_dt" />
		<result property="updateDt" column="update_dt" />
		<result property="createBy" column="create_by" />
		<result property="updateBy" column="update_by" />
	</resultMap>

	<select id="queryObject" resultType="com.power.entity.AgenciesEntity">
		select * from agencies where
		id = #{value}
	</select>

	<select id="queryList" resultType="com.power.entity.AgenciesEntity">
		select * from agencies
		<choose>
			<when test="sidx != null and sidx.trim() != ''">
				order by ${sidx} ${order}
			</when>
			<otherwise>
				order by id desc
			</otherwise>
		</choose>
		    <if test = "authAgenciesId !=null and authAgenciesId.trim() != ''" >
			authAgenciesId in ${authAgenciesId}
			</if>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="queryTotal" resultType="int">
		select count(*) from agencies
		    <if test = "authAgenciesId !=null and authAgenciesId.trim() != ''" >
			authAgenciesId in ${authAgenciesId}
			</if>
	</select>

	<insert id="save" parameterType="com.power.entity.AgenciesEntity"
		useGeneratedKeys="true" keyProperty="id">
		insert into agencies
		(
		`name`,
		`abbr_code`,
		`region`,
		`address`,
		`status`,
		`type`,
		`parent`,
		`create_dt`,
		`update_dt`,
		`create_by`,
		`update_by`
		)
		values
		(
		#{name},
		#{abbrCode},
		#{region},
		#{address},
		#{status},
		#{type},
		#{parent},
		#{createDt},
		#{updateDt},
		#{createBy},
		#{updateBy}
		)
	</insert>

	<update id="update" parameterType="com.power.entity.AgenciesEntity">
		update agencies
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="abbrCode != null">`abbr_code` = #{abbrCode}, </if>
			<if test="region != null">`region` = #{region}, </if>
			<if test="address != null">`address` = #{address}, </if>
			<if test="status != null">`status` = #{status}, </if>
			<if test="type != null">`type` = #{type}, </if>
			<if test="parent != null">`parent` = #{parent}, </if>
			<if test="createDt != null">`create_dt` = #{createDt}, </if>
			<if test="updateDt != null">`update_dt` = #{updateDt}, </if>
			<if test="createBy != null">`create_by` = #{createBy}, </if>
			<if test="updateBy != null">`update_by` = #{updateBy}</if>
		</set>
		where id = #{id}
	</update>

	<delete id="delete">
		delete from agencies where id = #{value}
	</delete>

	<delete id="deleteBatch">
		delete from agencies where id in
		<foreach item="id" collection="array" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</delete>

</mapper>